#include"main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: The number to be evaluated
 * Return: distance between 0 & the given number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
