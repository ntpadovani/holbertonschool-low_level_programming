#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alfabeto;
	for (alfabeto = 'z'; alfabeto >= 'a'; alfabeto--)
	{
		putchar(alfabeto);
	}
		putchar('\n');
	return (0);
}

