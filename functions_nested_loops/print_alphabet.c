#include"main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alfabeto;
	for (alfabeto = 'a'; alfabeto <= 'z'; alfabeto++)
	{
		_putchar(alfabeto);
	}
		_putchar('\n');
return (0);
}
